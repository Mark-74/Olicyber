Il file .pkt mostra moltissime richieste allo stesso servizio, tra queste ci sono alcune con metodo POST interessanti, quindi filtro per le richieste POST con http.request.method=="POST". Trovo quindi un pacchetto che contiene un file zip protetto da password (il file è scaricabile da wireshark cliccando sull'header [MIME multipart Media Encapsulation], poi sul secondo header con nome [Encapsulated multipart part] perchè ce ne sono due, e infine su Data posso fare sia [Show packet bytes] per un'anteprima oppure [Extract packet bytes], scelgo quest'ultima opzione e salvo il file). La password è rintracciabile seguendo il flusso TCP di un pacchetto TCP inviato poco dopo alla richiesta POST, in cui i 2 enti si scambiano la password perchè uno se la era dimenticata (bruh). Sblocco quindi il file zip e ottengo la flag: flag{l'1n6r3d13n73_536r370_è_am0r3..e_p4t4t3}
